<?php
/**
 * Hooks allows to modify the {{ name|capitalize }} Grid presenter.
 *
 * This hook is a right place to do some formatting operations (like currencies and dates).
 *
 * @param array $params
 */
 public function hookAction{{ id|capitalize }}GridPresenterModifier(array $params)
 {

 }
